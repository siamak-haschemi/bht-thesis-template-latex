# Simple Makefile to export SVGs from figures.drawio, commit, and push.

PY      := python3
SCRIPT  := ./export-drawio-svg.py
DRAWIO  := figures.drawio

# Main command: export SVGs, commit, and push (automatic workflow)
svg:
	$(PY) $(SCRIPT) $(DRAWIO)
